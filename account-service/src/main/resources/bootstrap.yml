spring:
  application:
    name: account-service
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        group: gjob
        file-extension: yml
      discovery:
        server-addr: 127.0.0.1:8848
        group: gjob
server:
  port: 7001
  servlet:
    context-path: /account